import csv
import os

GPIO.setmode(GPIO.BCM)

os.chdir('give the path where file to be saved')

mylist=[] #this will be my list

with open('sample.csv', newline='') as myFile:  
    reader = csv.reader(myFile)
    for row in reader:
        mydata=int(row[0])
        mylist.append(mydata)
        #GPIO.output(18, mydata) #18 is my output pin # here now i am appending my values to a list and later i will loop through the values
        print(mydata)

length=len(mylist)

for i in range(length):
    GPIO.output(18, mylist[i]) #will output the data at the ith index


